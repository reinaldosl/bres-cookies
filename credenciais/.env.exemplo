SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co

# Usada pelos scripts Python (Contador e Analista) — nunca expor no frontend
SUPABASE_KEY=sua_service_role_key_aqui

# Usada pelo site (Desenvolvedor Web) — chave pública, pode ir no frontend
SUPABASE_ANON_KEY=sua_anon_key_aqui
